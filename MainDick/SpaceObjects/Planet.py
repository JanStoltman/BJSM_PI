class Planet:

    def __init__(self, name, mass, radius, coordinates, image):
        self.name = name
        self.mass = mass
        self.radius = radius
        self.coordinates = coordinates
        self.image = image
